name: smartpumpmonitor
description: Application de supervision de pompe solaire SmartPumpMonitor
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^2.24.2
  firebase_database: ^10.4.2
  flutter_map: ^6.1.0          # Remplace mhj_maps
  latlong2: ^0.9.0
  fl_chart: ^0.66.0
  intl: ^0.19.0
  provider: ^6.1.1
  flutter_local_notifications: ^17.0.0
  url_launcher: ^6.2.5
  equatable: ^2.0.5
  firebase_auth: ^4.16.0
  go_router: ^17.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true